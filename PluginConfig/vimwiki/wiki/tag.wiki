显示里程碑
git tag -n

创建一个含附注类型的里程碑
git tag -a v1.x -m "comment"

后期加注里程碑
git tag -a v1.x SHA1

删除里程碑
git tag -d name

本地所有里程碑全部推送到远程版本库
git push origin refs/tags/*

= 删除远程tag =
git push origin --delete tag <tagname>
